<br>
<h2>Reviews</h2>
<h3><%= @park.name %> National Park</h3>
<table class="data">
    <tr>
        <th>Title</th>
        <th>Park Name</th>
        <th>Date Created</th>
        <th>Rating</th>
    </tr>
            <% @park.posts.order(:created_at => :desc).each do |post| %>
    <tr>
            <td><a href="../posts/<%= post.id %>"><%= post.title %></a></td>
            <td><%= post.park.name %></td>
            <td><%= post.created_at %></td>
            <td><%= post.rating %></td>
    </tr>
    <% end %>
</table>
<h3><%= 'No reviews yet' if !@park.posts[0] %></h3>